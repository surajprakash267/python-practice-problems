# Python Functions Practice – Set 7

This repository contains solved Python programs related to **functions**, **arguments**, **scope**, **lambda**, **map/filter/reduce**, and more.

Each question is saved as a separate file for easy understanding.

---

## 📂 Folder Structure

python-functions-set/
│
├── README.md
├── definitions.md
└── programs/
├── 01_palindrome.py
├── 02_factorial.py
├── 03_args_sum.py
├── 04_return_multiple.py
├── 05_lambda_square.py
├── 06_lambda_max.py
├── 07_map_filter_reduce.py
├── 08_default_arguments.py
├── 09_fibonacci.py
└── 10_closure.py

---

## 📘 Program Descriptions

### **1. Palindrome Check**
Converts input to lowercase, removes spaces, and checks if it reads the same backwards.

### **2. Factorial (Recursion)**
Uses recursion to compute factorial and handles negative numbers safely.

### **3. *args Sum Function**
Demonstrates variable-length arguments.

### **4. Return Multiple Values**
Function returns name, age, city as a tuple.

### **5. Lambda – Square**
Anonymous function to square a number.

### **6. Lambda – Max of Two**
Inline lambda returning the larger of two numbers.

### **7. map(), filter(), reduce()**
Functional programming examples:
- `map()` → apply transformation  
- `filter()` → select items  
- `reduce()` → accumulate results  

### **8. Default Argument Values**
Function with default parameter (`greeting="Hello"`).

### **9. Fibonacci Recursion**
Classic recursive Fibonacci function.

### **10. Closure Example**
Inner function uses variables from outer function.

---

## 📄 Additional Reference
For definitions of concepts like:
- lambda
- map
- filter
- reduce
- recursion
- closures

See **definitions.md**.
